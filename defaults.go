package caveman

// TODO: we should have some functions which provide a lot of decent defaults - so you can build
// a fully functional file-only site, or database-only site with just one or two calls.  And then
// individual parts can be overridden, the request chain can be modified, etc. or the source
// from here can just be copied out and customized as needed.

func NewDefaultFSOnlySite() {

}

func NewDefaultDBOnlySite() {

}

// pages are in db but the rest is in fs? - figure out the variations here...
func NewDefaultDBBlogSite() {
}
